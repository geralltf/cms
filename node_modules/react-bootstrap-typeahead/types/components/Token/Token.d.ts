import React from 'react';
import { UseTokenProps } from '../../behaviors/token';
export interface TokenProps<T> extends UseTokenProps<T> {
    disabled?: boolean;
    readOnly?: boolean;
}
declare const Token: ({ children, option, readOnly, ...props }: TokenProps<HTMLElement>) => React.JSX.Element;
export default Token;
